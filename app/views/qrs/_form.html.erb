<%= form_with(model: qr, local: true) do |form| %>
  <% if qr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(qr.errors.count, "error") %> prohibited this qr from being saved:</h2>

      <ul>
      <% qr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
